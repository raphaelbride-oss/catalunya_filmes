# Painel financeiro

Acesso restrito. Conteúdo criptografado (AES-256-GCM + PBKDF2-SHA256).

O `index.html` é o único artefato deste repo — gerado a partir do workspace
agregador (não-versionado) que contém os scripts de extração, parse e build.
